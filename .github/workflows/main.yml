import 'package:flutter/material.dart';
import 'package:hive_flutter/hive_flutter.dart';

void main() async {
  // Ensure Flutter is initialized for hardware-level calls
  WidgetsFlutterBinding.ensureInitialized();

  // Initialize the Sovereign Data Vault (Hive) 
  // This replaces the cloud. Data stays on your iron.
  await Hive.initFlutter();
  
  // Open the primary telemetry vault
  await Hive.openBox('nexus_telemetry');

  runApp(const HVFNexusApp());
}

class HVFNexusApp extends StatelessWidget {
  const HVFNexusApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'HVF Nexus Core',
      theme: ThemeData(
        brightness: Brightness.dark, // Executive Theme
        primaryColor: Colors.blueGrey[900],
        scaffoldBackgroundColor: Colors.black,
      ),
      home: const Dashboard(),
    );
  }
}

class Dashboard extends StatelessWidget {
  const Dashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('HVF NEXUS CORE V2'),
        backgroundColor: Colors.blueGrey[900],
        centerTitle: true,
      ),
      body: Container(
        padding: const EdgeInsets.all(20),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(
              Icons.shield_rounded,
              size: 100,
              color: Colors.greenAccent,
            ),
            const SizedBox(height: 20),
            const Text(
              'SYSTEM STATUS: LIVE',
              style: TextStyle(
                fontSize: 28, 
                fontWeight: FontWeight.bold, 
                color: Colors.greenAccent,
                letterSpacing: 2.0,
              ),
            ),
            const Divider(color: Colors.blueGrey, height: 40),
            const Text(
              'SOVEREIGN DATA MODE ACTIVE',
              style: TextStyle(fontSize: 16, color: Colors.blueGrey),
            ),
            const SizedBox(height: 10),
            const Text(
              'LOCAL NODE: S-NODE 001',
              style: TextStyle(fontSize: 14, color: Colors.white70),
            ),
          ],
        ),
      ),
    );
  }
}
